/** Vertical spacing for label → field → helper → error stacks in Medmo forms. */
export const formFieldGroupClassName =
  "flex flex-col gap-[var(--space-stack-xs)]";

/** Checkbox / radio option row: control + label + optional description. */
export const formOptionRowClassName =
  "flex items-start gap-[var(--space-inline-sm)]";

/** Nested copy beside a checkbox or radio control. */
export const formOptionContentClassName =
  "flex min-w-0 flex-col gap-[var(--space-stack-xs)]";

/** Group of checkbox or radio options below a field label. */
export const formOptionListClassName =
  "flex flex-col gap-[var(--space-stack-sm)]";
